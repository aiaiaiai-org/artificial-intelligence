# © 2026 aiaiaiai · aiaiaiai.org
# SPDX-License-Identifier: Apache-2.0

name: Rust CI

on:
  # Every pull request, whatever it targets. A change stacked on another branch has to
  # prove itself before that branch merges, not after.
  pull_request:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  correctness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Toolchain
        run: rustc --version && cargo --version
      - name: Bootstrap lockfile
        run: test -f Cargo.lock || cargo generate-lockfile
      - name: Format
        run: cargo fmt --all --check
      - name: Clippy
        run: cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
      - name: Test
        run: cargo test --locked --workspace --all-features
      - name: Architecture dependency boundary
        run: python3 scripts/check_architecture.py

  # A product compiles these crates into its own browser artifact, so losing the target is
  # its blocker, not ours — and it would surface only when that product next tried to build.
  browser-target:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Toolchain
        run: rustup target add wasm32-unknown-unknown
      - name: Bootstrap lockfile
        run: test -f Cargo.lock || cargo generate-lockfile
      - name: Build for wasm32-unknown-unknown
        run: >
          cargo build --locked --target wasm32-unknown-unknown
          -p aiai-contracts -p aiai-runtime -p aiai-signal

  dependency-policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Bootstrap lockfile
        run: test -f Cargo.lock || cargo generate-lockfile
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
