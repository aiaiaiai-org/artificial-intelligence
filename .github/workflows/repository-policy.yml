# © 2026 aiaiaiai · aiaiaiai.org
# SPDX-License-Identifier: Apache-2.0

name: Repository policy

on:
  # Every pull request, whatever it targets. A change stacked on another branch has to
  # prove itself before that branch merges, not after.
  pull_request:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate license, SPDX, and repository legal baseline
        run: python scripts/check_repository_policy.py Apache-2.0
